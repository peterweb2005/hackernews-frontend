import styled from 'styled-components';

const Title = styled.h1`
  font-size: 1.5em;
`;

const SubTitle = styled.h3`
  font-size: 1.2em;
`;

const SelectContainer = styled.section`
  padding: 2em;
`;

export { Title, SubTitle, SelectContainer };